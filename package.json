{
  "devDependencies": {
    "vitepress": "^1.6.3"
  },
  "scripts": {
    "updateVersion": "node scripts/fetch-latest-release.js",
    "prebuild": "node scripts/fetch-latest-release.js",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  }
}